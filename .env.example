# ===========================================
# 🌍 Environment configuration
# ===========================================
# Entorno actual de la app:
# - "development" → usa credenciales locales (.env)
# - "production"  → usa Role IAM en AWS (sin credenciales)
ENVIRONMENT=development


# ===========================================
# 🔐 AWS Configuration
# ===========================================
# Región donde se usarán los servicios AWS
AWS_REGION=us-east-2

# Claves de acceso AWS (solo necesarias en desarrollo local)
# En producción (Lambda/ECS), se usan credenciales del rol IAM automáticamente
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_KEY
